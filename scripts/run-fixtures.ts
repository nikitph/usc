// Built by packet K-06. Until then: count fixtures and fail if runners are missing for any present runner kind.
// This placeholder intentionally FAILS once kernel fixtures exist and no runner is registered —
// so 'make verify' cannot be green by omission.
console.error("fixture runner not yet implemented (packet K-06)"); process.exit(1);
