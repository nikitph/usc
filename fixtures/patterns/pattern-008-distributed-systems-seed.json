{
  "runner": "pattern",
  "name": "distributed systems twin-domain seed direction",
  "operation": "twinDomainSeedBundle",
  "expect": {
    "domain": "distributed-systems",
    "patterns": ["pattern:distributed-systems:circuit-breaker"],
    "antiPatterns": ["anti-pattern:distributed-systems:stale-cache-contract"],
    "reviewState": "pending"
  },
  "adjudication": "Approved as a seed direction for exercising the USC transfer pipeline, not as an adjudication of domain fitness. Domain-specific failure modes are owned by downstream users/operators through feedback. Generated artifact bundles remain examples, not live ground truth."
}
