# SPEC — Pattern Plane (workers/offline + KG tables)

Blueprint §7–§8. STATUS: MVP 3 — packets not yet authored. This stub reserves the
boundary; DO NOT implement against it yet. Will cover: pattern/anti_pattern body schemas,
WL canonical hashing [VARIATION-POINT: dedup hasher], fidelity scorer + calibration sets,
review queue, richness ladders, hybrid retrieval. Python workers; Pydantic models come
from /schemas codegen only.
